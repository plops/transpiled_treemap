# Build outputs must never be committed (Rust target/, CMake build dirs,
# transpiler staging dir — cpp/gen/ IS committed as review artifact).
/target/
iter2/target/
cpp/build/
build/
basan/
iter2/lisp/gen/
# Transient editor and backup files.
*~
*.known-good
