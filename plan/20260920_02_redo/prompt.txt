du arbeitest im auftrag von wol pumba (wolpumba@gmail.com)

lies dir lan/20260919_01_merge/rust_cpp_vergleich.md durch.
ich moechte eine zweite iteration durchfuehren.
zurerst verschiebe (git mv) den existierenden rust und cpp code in einen ordner iter1 (oder so) und starte mit einem neuen ordner iter2.
wie im letzten plan (20260919_01_merge/...) soll zuerst direkt rust code erzeugt werden, dieser getestet und gebenchmarkt. dann semantisch equivalenter c++ code
(auch testen und benchmarken). wenn das alles geht eine transpilierte reimplementierung vom c++ code machen.

du sollst wieder die parallele version implementieren. aber immer mit der seriellen referenz vergleichen.

zwischendurch committen. am ende ein walkthrough.md dokument im plan/20260920... ordner anlegen und auch einen report mit einem vergleich zwischen der neuen c++ und rust implementierung.
koennen wir nun sicherstellen dass der c++ code mit den variablen genauso umgeht wie der rust code? erbt der c++ daher seine sicherheit vom rust code?

stelle sicher das numerierte scripte vom namen pattern wie setup01_<name>.sh existieren um 'rust code zu compilieren', 'rust code testen/benchmarken' (vielleicht zwei scripte), 'direkten c++ code zu compilieren', 'direkten c++ code testen/bench', 'c++ transpilat erstellen', 'transpilat compilieren', 'transpilat testen', 'transpilat benchmark' 
 
die common-lisp-aehnliche transpiler input sprache ist hier erklaert: /workspace/src/cl-cpp-generator2/SUPPORTED_FORMS.md

beim erzeugen von tranpiler input:
mit dem setzen von klammern musst du wirklich vorsichtig sein!
nutze das tool /workspace/src/parenmedic/zig-out/bin/parenmedic um klammern oft zu checken und zu fixen.
wenn du dich vertust, dann ist es sehr schwierig den fehler zu korrigieren. besser ist es vor einer aenderung ein (known-good) backup zu machen und zu diesem zurueck zu gehen, falls es fehler mit klammern gibt. 
i suggest that you should write
  functions that are no longer than 60 lines, you can have several such functions in a file but they should be at the top level or with closing parens on individual lines. this allows me to check the parens in emacs by looking at the highlighted matches on the screen. for parens outside of
  functions (like `(do0 ..) or other top level stuff ) place closing parens on individual lines


du kannst xvfb fuer tests nutzen, siehe 20260918_02_review_and_xvfb_test/walkthrough.md fuer details


nutze tools um konsistenten und aufgeraeumten quellcode zu gewaehrleisten. ich moechte den code und die abhaengigkeiten moeglichst klein halten.


Schaue dir den Quellcode an und die Architektur dieser Software an. Nutze deepwiki mcp fuer plops/cl-cpp-generator2 um dokumentation zu bekommen und fragen beantworten zu lassen. falls abhaengigkeiten eingefuehrt werden sind diese mitsamt ihrer github organisation in deps.md aufzuschreiben, so dass wir spaeter einfach die deepwiki abfragen konstruieren koennen. nutze rust tools um auf die neueste vorhandene version zu switchen (auch wenn vor kompatibilitaet gewarnt wird, wenn wir gerade eine dependency einfuehren ist es wichtig, die neueste version zu nehmen) und schon vorab usage examples in den plan aufzunehmen (mit deepwiki mcp zu erfragen)

du befindest dich in einem docker container, basierend auf ubuntu 26. du darfst neue programme installieren, falls noetig. 

Schlage vor, wie wir dieses Feature implementieren können. Habe ich alle Requirements aufgelistet, die wichtig sind, oder kannst du dir vorstellen, dass noch andere Dinge werden sollen? Liste mir diese auf Vorschlägen, was man am besten umsetzen könnte.

Schreibe einen Implementierungsplan. Dieser Implementierungsplan soll einen unabhängigen AI-Agenten dazu befähigen, selbstständig einen Kontext aufzubauen, der die wesentlichen
quellen beinhaltet. Das heißt, der Implementierungsplan hat eine Liste von Dateien, die sich der Agent sich anschauen sollte, mit einer kurzen Beschreibung für jede Datei.
Weiterhin soll der Implementierungsplan eine Erklärung Commit Messages sendet werden sollen. Und zwar sollen diese mit einer umfassenden Beschreibung und als Conventional Commit format ausgeführt werden.

Neue Unit-Tests und Integration-Tests sollen wie erforderlich eingeführt werden.
Diese sollen auch ausgeführt werden.

schreibe task.md wo du die implementierungs und test schritte aufeinander abfolgen laesst, so dass ein agent die tasks seriell abarbeiten kann und die arbeit in jedem schritt getested und validiert wird.

Nach der vollständigen Implementierung, wenn alle Tests abgeschlossen sind und alles committed ist, soll auch ein Walkthrough-Dokument entgeschrieben werden und 
in plan/2026.../walkthrough.md abgelegt werden. Dieses soll zusammenfassen, was wirklich implementiert wurde, welche Stellen aufgrund der Tests oder wie auch immer gemacht wurden, als im Untergrund auch Learnings und mögliche Erweiterungen. liste auch die neuen programme die mit in den docker container aufgenommen werden sollten 
